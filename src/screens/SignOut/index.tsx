import React, { Fragment } from 'react'

function SignOut(): JSX.Element {
	// Aplicar todas as regras para limpar dados da store e desconectar o usuário

	return (
		<Fragment>
			<h1>Você está sendo desconectado</h1>
		</Fragment>
	)
}

export default SignOut
